# NEON DEPTHS — 브라우저 로그라이크

단일 `index.html`로 동작하는 턴제 로그라이크. 다운로드 후 더블클릭하면 바로 실행된다.
외부 라이브러리/CDN/빌드 도구 없음. Canvas 단일 렌더링, 60fps.

- **플레이**: `index.html`을 브라우저에서 열기
- **검증**: `node test.js` (index.html에서 게임 로직을 추출해 헤드리스로 돌리는 스모크 테스트)

저장소의 다른 파일(수학 연구 자료)과 게임은 무관하다. 게임 산출물은 `index.html` 하나이고,
`CLAUDE.md`와 `test.js`는 개발/검증용이다.

## 목표 루프

지하 10층에 잠든 **네온 하트**를 손에 넣으면 승리. 층마다 절차 생성된 던전을 내려가며
전투 → 장비/레벨 성장 → 자원(포션) 관리 → 사망 시 퍼머데스 → 즉시 재시작.
1~2층은 튜토리얼 난이도, 3층부터 긴장, 5층+는 빌드가 나쁘면 죽고, 10층엔 보스(심장의 수호자)가 있다.
한 런은 대략 30~45분.

## 아키텍처

논리와 연출을 분리한 고전 로그라이크 구조:

- **턴 로직은 즉시 해소** — 입력 1회 = 플레이어 1행동 → 모든 몬스터 1행동 → 턴 종료 틱(버프/독/재생).
  게임 상태는 키 입력 시점에 완전히 결정된다.
- **렌더 루프는 항상 60fps** — `requestAnimationFrame`에서 보간만 담당.
  엔티티는 논리 좌표 `(x,y)`와 렌더 좌표 `(rx,ry)`를 따로 가지며 rx,ry가 지수 감쇠로 따라온다(스냅 금지).
  화면 흔들림/파티클/데미지 팝업/투사체/플래시 전부 렌더 측 코스메틱 데이터.

### index.html 내부 섹션 순서 (배너 주석으로 구분)

1. `ENV/RNG` — mulberry32 시드 RNG. 게임 전체가 `Math.random` 대신 이걸 쓴다(헤드리스 재현 테스트용).
2. `CONFIG/BALANCE` — 타일/맵 크기, 층 테마, 몬스터/장비/아이템/성장 테이블. 숫자는 전부 여기 모여 있다.
3. `AUDIO` — Web Audio 절차 사운드(아래 참조).
4. `SPRITES` — 12×12 픽셀맵 문자열 → 오프스크린 캔버스로 부트 시 1회 프리렌더(런타임 픽셀 드로잉 없음 = 60fps).
5. `MAPGEN` — 룸 배치 + L자 복도.
6. `FOV/LOS` — 재귀 섀도캐스팅, Bresenham 시선.
7. `EFFECTS` — 메시지 로그, 팝업, 파티클, 흔들림, 스플래시.
8. `COMBAT/LEVEL` — 데미지 공식, 경험치/레벨업.
9. `ACTIONS/TURN/AI` — 플레이어 행동, 턴 진행, 몬스터 AI.
10. `ITEMS/INVENTORY` — 줍기/사용/장착/버리기.
11. `INPUT` — 키맵 + 키 홀드 반복(105ms 쿨다운).
12. `RENDER` — 카메라, 타일/엔티티/UI/미니맵/오버레이.
13. `BOOT` — 브라우저에서만 실행. 헤드리스(node)에서는 정의만 로드되고 `globalThis.__neon`으로 로직이 노출된다.

게임 상태는 단일 객체 `G` (state machine: `title → play ⇄ inv → dying → dead`, `win`).

## 핵심 설계 결정과 근거

| 결정 | 근거 |
|---|---|
| 룸 배치 + L자 복도 (BSP 대신) | 구현이 단순하고 견고. 방을 순서대로 복도로 이으면 **연결성이 구조적으로 보장**된다. 테스트로 300층 연결성 검증. |
| FOV = 재귀 섀도캐스팅 8옥턴트, 반경 8 | 표준 알고리즘, 벽 차단이 정확하고 빠름. `seen[]`(영구 기억) + `visible[]`(매 턴 재계산)으로 fog of war. |
| 추적 AI = 플레이어 기준 BFS 거리장 1회/턴 | 모든 몬스터가 거리장 내리막으로 이동 → 코너를 돌아 자연스럽게 추적, 비용 O(맵) 한 번. 몬스터별 경로탐색 불필요. |
| 이동 4방향 (망령만 8방향) | 조작 스펙이 4방향. 망령의 8방향 이동은 종류 차별화 요소. |
| 10층 유한 목표 + 보스 | "최소 5층"을 넘어서 명확한 클라이맥스와 승리 화면을 제공. 무한 모드보다 30분+ 런에 적합. |
| 자연 재생 1HP/8턴 (독 상태에선 정지) | 포션을 "아껴야 하는 비상 자원"으로 유지하면서, 포션이 안 나오는 불운만으로 런이 망하지 않게 함. |
| 회복 포션은 HP가 가득하면 소모되지 않음 | 실수 보호. 낭비감 대신 자원 관리 재미에 집중. |
| 층 이동은 내려가기 단방향 | 이전 층 상태를 버릴 수 있어 상태 관리 단순화. 로그라이크 관례. |
| 텍스트: 메시지/아이템명 한국어, 라벨(HP/LV/FLOOR)·타이틀 영어 | 레트로 터미널 감성 + 사용자 가독성. 이모지 금지, 모노스페이스. |
| 사운드 전부 오실레이터/노이즈 절차 생성 | 외부 파일 금지 제약. 첫 키 입력에서 AudioContext 생성/resume(자동재생 정책). M으로 음소거. |
| 사망 연출 = 물리 바운스(중력+회전) + "boing" 사운드 3연타 | 스펙의 "과장되고 우스꽝스럽게". 1.4초 후 통계 화면. |

## 밸런스

### 플레이어
- 시작: HP 30, ATK 3, DEF 0, 크리티컬 10%(×2)
- 데미지 공식: `max(1, ATK − DEF + rand(0..2))`, 몬스터 크리티컬 5%
- 레벨업: maxHP +5, 50% 회복, 짝수 레벨 ATK +1, 3의 배수 레벨 DEF +1
- 경험치 요구: `10 + (lv−1)·10 + (lv−1)²·2` → 10층 도달 시 대략 LV 8~10

### 몬스터 (기본치 / 출현층, 깊을수록 HP +12%/층·ATK 보정)
| 이름 | HP/ATK/DEF/XP | 층 | 패턴 |
|---|---|---|---|
| 쥐 | 6/3/0/4 | 1+ | 근접 추적 |
| 박쥐 | 4/2/0/4 | 1+ | **빠름(2행동)** + 50% 변칙 이동 |
| 슬라임 | 14/4/1/7 | 2+ | **느림(격턴)**, 단단함 |
| 독거미 | 9/3/0/8 | 2+ | 명중 시 60% 독(1dmg×5턴) |
| 해골 궁수 | 9/5/0/10 | 3+ | 사거리 6+LOS 사격, **사격 후 1턴 재장전**, 인접 시 후퇴(35%는 근접 반격) |
| 폭탄버섯 | 8/—/0/10 | 3+ | 인접 시 점화(1턴 예고) 후 **자폭** 3×3, 다른 몬스터도 휘말림 |
| 망령 | 14/6/1/14 | 5+ | 8방향 이동, 준 피해만큼 흡혈 |
| 골렘 | 30/9/3/22 | 5+ | 느림, 강타 |
| 악마 | 22/8/2/18 | 7+ | 강한 추적형 |
| 심장의 수호자 | 80/12/4/50 | 10 보스 | 하트 옆을 지킴, 보통 속도 |

몬스터 수: `5 + round(층×1.2)` (1층 6 → 10층 17 + 보스). 플레이어 시작 지점 반경 8 안엔 스폰 금지.
기상 규칙: 플레이어 **시야에 들어오면 기상**, 전투 소음(공격)은 반경 5의 잠든 몬스터를 깨움.

> 밸런스 반복 기록: 초기 궁수는 "유클리드 2.2 이내면 후퇴" 카이팅이었는데, 같은 속도로 영원히
> 도망치며 쏘지도 못하는 무한 대치가 가능했다(그리디 봇이 재현). 인접 시에만 후퇴 + 재장전
> 사이클로 교체. 사인 점유율 70%→53%로 완화되면서도 시그니처 위협 유지.

### 아이템
- 무기: 단검+2 / 장검+4 / 전투도끼+6 / 카타나+8 / 둠블레이드+11 (층수에 비례한 티어 드랍)
- 방어구: 가죽+1 / 사슬+3 / 판금+5 / 이지스+7
- 포션: 체력(40% 회복, 최소 15) / 분노(+5 ATK, 30턴) / 철벽(+5 DEF, 30턴)
- 스크롤: 순간이동(무작위 재배치) / 지도(층 전체 공개) / 낙뢰(보이는 가장 가까운 적에게 16+층×2)
- 층당 3~4개 드랍, 1~2층은 체력 포션 1개 보장. 인벤토리 10칸.

## 조작

이동 `WASD/방향키` · 대기 `Space` · 줍기 `G` · 인벤토리 `I` (Enter 사용/장착, D 버리기, Esc 닫기) ·
계단 `E` · 음소거 `M` · 사망/승리 후 `R` 재시작

## 게임 필 체크리스트

- [x] 피격 시 화면 흔들림(피해량 비례) + 피격 엔티티 흰색 플래시
- [x] 데미지 숫자 팝업: 떠오르며 페이드, 크리티컬은 크고 노랗게
- [x] 이동/공격 런지 보간 (rx,ry 지수 감쇠 — 스냅 없음)
- [x] 절차 사운드: 타격/크리/피격/줍기/포션/장착/레벨업/계단/낙뢰/폭발/화살/독/사망(boing)/승리 팬파레
- [x] 레벨업: 금색 링 파티클 + 스플래시 + 팬파레
- [x] HP바 고스트(흰 잔상이 천천히 따라 줄어듦), 저체력 붉은 비네트 펄스
- [x] 층 진입 페이드 + "B{n}F — 테마명" 스플래시, 층마다 액센트 색상 테마
- [x] 사망: 바운스 물리 + boing, 통계(층/킬/턴/시간/사인) + R 재시작

## 검증 (test.js — `node test.js`)

1. **맵 생성 ×300** (층 1~10): 플레이어→계단(10층은 하트) BFS 도달 가능, 모든 엔티티/아이템이 바닥 타일 위, 경계 침범 없음
2. **FOV**: 벽 차단(벽 뒤 타일 비가시), 자기 타일 가시, 반경 준수
3. **전투 경계값**: 2000회 공격 시뮬 — 데미지 ≥1, HP 음수 없음, 사망 1회만 발생
4. **봇 시뮬**: 30시드 × 최대 3000턴 무작위 행동(이동/줍기/사용/계단) — 예외 0건, NaN 없음,
   몬스터 벽/중첩 없음, 사망·승리 상태 정상 도달
5. **인벤토리 엣지**: 가득 찬 인벤 줍기 거부, 장착 교체 시 스탯 재계산, 만피 회복 포션 미소모, 빈 인벤 열기
6. **전체 루프**: 시작→전투→레벨업→사망→재시작이 상태 머신 상 끊김 없이 순환 (봇 시뮬 + 수동 점검)

검증 결과는 이 파일 하단에 기록한다.

## 마일스톤

1. ✅ CLAUDE.md 설계 기록
2. ✅ 렌더링 골격 (캔버스, 스프라이트 프리렌더, 카메라)
3. ✅ 던전 생성 + 이동/FOV
4. ✅ 전투 + 몬스터 AI + 사망
5. ✅ 아이템/인벤토리/장비 + 성장
6. ✅ 폴리시 (사운드, 파티클, 화면 연출, 타이틀/사망/승리 화면, 밸런스)
7. ✅ 헤드리스 검증 + 전체 루프 점검

## 검증 결과 (2026-06-10)

`node test.js` — **12,908 체크 통과, 0 실패**

| 항목 | 결과 |
|---|---|
| 맵 생성 ×300 (30시드 × 10층) | 전 층 플레이어→목표 BFS 도달, 배치 불변식 위반 0 |
| FOV | 벽 차단/반경/seen 기록 모두 통과 |
| 전투 2000회 ×2 | 피해 범위 `[1, (atk-def+2)×2]` 유지, 사망 1회만 처리 |
| 성장 | 레벨업 스탯/초과 XP 이월 정상 |
| 인벤토리 엣지 | 가득참 거부·만피 미소모·장착 교체·대상 없는 낙뢰 미소모·빈 인벤 안전 |
| 무작위 봇 30시드 | 예외 0, NaN 0, 몬스터 벽/중첩 0 |
| 브라우저 경로 (canvas 스텁) | boot→타이틀→플레이→인벤→사망 연출→재시작→승리 전 상태 통과 |

난이도 곡선 (그리디 봇 24시드, 회복 45% 임계·장비 자동 교체·전부 전투):
- **승리 7 + 10층 승리 직전 5** (봇의 인벤 가득參 한계) ≈ 실효 50%
- 사망 분포: B4F×1 B5F×1 B6F×1 B7F×1 B8F×3 B9F×3 B10F×2 — **1~3층 사망 0**
- 사인: 해골 궁수 58%, 나머지 독/골렘/수호자/독거미/쥐 분산
- 평균 도달 레벨 9.5, 평균 체력 물약 소비 4.3개

사람 기준으로는 봇보다 초반 실수가 많고 후반 전술(코너 활용, 후퇴)이 좋으므로
"1~2층 무난, 3층부터 긴장, 5층+ 빌드 검증, 10층 보스전"의 의도 곡선에 부합한다.
